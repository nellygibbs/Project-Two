<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>View a Date</title>
</head>

<body class="show-body">
    <h1>Details for date at <%= date.location %></h1>
    <div class="show-ul">
        <ul class="show">
            <li>Cost: <%= date.cost %></li>
            <li><img src="<%= date.img %>"/></li>
            <% if(date.completed) { %>
                    <li>You already went here</li>
                <% } else { %>
                    <li>You haven't been here yet..</li>
            <% } %>
            <a href="/dates">Back to Date Wishlist</a>
       </ul>
    </div>
</body>
</html>