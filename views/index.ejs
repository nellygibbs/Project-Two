<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Anton&display=swap" rel="stylesheet">
    <title>Date Night Wishlist</title>
</head>
<body class="index-body">
    <h1 class="head">Date Night Wishlist</h1>
    <ul class="ul">
        <% dates.forEach(date => { %>
            <li class="li">
                <a href="/dates/<%= date._id %>" class="name">
                    <%= date.location %><br></a> 
                    <span class="img"><img src="<%= date.img %>"/></span><br>
                <form action="/dates/<%= date._id %>?_method=DELETE" method="POST">
                    <input type="submit" class="btn" value="delete">
                </form>
                <a href="/dates/<%= date._id %>/edit">Want to make changes?</a>

            </li>
      <% }) %>
    </ul>
    <a href="/dates/new">Add a New Date</a>
</body>
</html>